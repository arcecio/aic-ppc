{
  "name": "aip-ppc-console",
  "version": "0.1.0",
  "private": true,
  "description": "Local control plane: start/stop/health for postgres, backend, frontend, TEI.",
  "scripts": {
    "dev": "concurrently -n srv,web -c blue,green \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "build": "npm --prefix web run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
