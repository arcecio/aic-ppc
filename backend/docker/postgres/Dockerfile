# Postgres 16 base for the AIP PPC Assistant. Kept as a dedicated image so the
# knowledgebase layer can later add pgvector (semantic retrieval over the
# regulatory corpus) without touching docker-compose — mirrors the Blue pattern.
FROM postgres:16
