package com.lacity.aipppc.model.enums;

/** Project record lifecycle. Advisory only — the tool never issues permits. */
public enum ProjectStatus {
    DRAFT, INTAKE, SCREENING, SCREENED, SUBMITTED_TO_EPLANLA, ARCHIVED
}
